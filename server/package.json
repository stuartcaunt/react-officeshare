{
  "name": "officeshare-server",
  "version": "1.0.0",
  "scripts": {
    "start": "node src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "supervisor -e \"js,json\" -w src -- src/server.js"
  },
  "dependencies": {
    "express": "^4.16.4",
    "node-uuid": "^1.4.8",
    "socket.io": "^2.2.0"
  },
  "devDependencies": {
    "supervisor": "^0.12.0"
  }
}
